#include "AIPlayer.h"

void getChild(size_t i); //get ith children
void getChild(); //get all children
void setChild(size_t i); //ith children to a different state
void addChild(); //
void generateChildren //

// main method for the AI to determine which column to place in
size_t move(const Board b){
  // return BFS(b);
  // return DFS(b);
  // return MiniMax(b);
  // return IterativeDeepening(b);
 }
