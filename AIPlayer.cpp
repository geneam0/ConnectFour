#include "AIPlayer.h"

//ith children to a different state
void AIPlayer::setChild(size_t i); 
void AIPlayer::addChild(); //
Item* AIPlayer::generateChildren();
// main method for the AI to determine which column to place in
size_t AIPlayer::move(const Board b){
  // return BFS(b);
  // return DFS(b);
  // return MiniMax(b);
  // return IterativeDeepening(b);
}
