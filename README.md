# ConnectFour
Functions we need:
   # print board function
   # check win
   # User turn (select a column to input a piece)
   #       inputing piece
   #       bool to check if column is full
   # Computer turn
   #      AI (easy, medium, hard)
